#!/usr/bin/expect

spawn /glassfish4/glassfish/bin/asadmin --user admin change-admin-password

expect "password"
send "\n"

expect "password"
send "{{ asadmin_password }}\n"

expect "password"
send "{{ asadmin_password }}\n"

expect eof
exit
